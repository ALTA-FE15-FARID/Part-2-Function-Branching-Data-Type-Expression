// Exponentiation

function computePower(x: number, n: number): number {
    return Math.pow(x, n);
  }
  
  // Output
  const result1: number = computePower(2, 3);
  console.log(result1); // Output: 8
  
  const result2: number = computePower(7, 2);
  console.log(result2); // Output: 49